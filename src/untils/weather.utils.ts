import {dayWeather, dayWeatherAPIResponse} from '../model/weather.model';

export const convertAPIResponseToDayWeatherList = (res: dayWeatherAPIResponse): dayWeather[] => {

  console.log("In convertAPIResponseToDayWeatherList, res=", res);
  return [];
}
